import Intro from './components/layout/home/intro';

const Home = () => {
    return (
        <main className="flex flex-col items-center">
            <Intro/>
        </main>
    );
};

export default Home;