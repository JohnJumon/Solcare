import ProfileDrawer from "./components/layout/profile/profileDrawer";
const Profile = () => {
    return(
        <main className="max-w-screen-xl mx-auto pl-0 text-black">
            <ProfileDrawer/>
        </main>
    );
}

export default Profile;